import React, { useEffect } from "react";

function LadingPage() {
  useEffect(() => {}, []);

  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        height: "100vh"
      }}
    >
      <h2>Landing Page</h2>
    </div>
  );
}

export default LadingPage;
